# AI Capability Builder Mock Site

This repository contains a single-page mock website that presents research design options for improving an AI capability building platform. The page renders sample data on load and has no external dependencies.

## Usage

Open `index.html` in a modern web browser to view the site.
